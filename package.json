{
  "name": "squad-web-client",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "firebase": "^10.12.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "build": "esbuild web/src/index.tsx --bundle --minify --sourcemap --outfile=web/dist/bundle.js --loader:.js=jsx --loader:.tsx=tsx --external:react --external:react-dom/client --external:firebase/app --external:firebase/database --format=esm"
  }
}
